//Accepts and sanitizes input text before it can be entered into database
//Rectagular form field
//Props: default text, text color, border color, background color

const TextInput = () => {
	return (
		<div>
			
		</div>
	)
}
export default TextInput;