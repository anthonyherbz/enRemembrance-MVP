const TableIcons = ({}) => {
	//number of icons, their names,per-icon color, per-icon display on/off
	//dimensions will be assumed/always the same

}



// 	if (count || name || dimensions === "undefined"){
// 		console.log("TableIcons requires count, name, and dimension props");
// 		return <div>TableIcons requires count, name, and dimension props</div>;
// 	}else{
// 		for (let i = 0; i < count.length(); i++){
// 			<Icon name={} />
// 		}
// 	}
// 	return (
// 		<>
			
// 		</>
// 	)
// }
export default TableIcons;